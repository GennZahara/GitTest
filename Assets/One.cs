using UnityEngine;

public class One : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // 안뇽 안뇽 안뇽 456 456
        int damage = 8;
        Debug.Log("다마게 : " + damage);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
